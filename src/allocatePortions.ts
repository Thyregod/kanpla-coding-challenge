import { getCheapestCombination } from './getCheapestCombination';
import { Bundle, Order, User } from './types';

export function allocatePortions(users: User[], bundles: Bundle[]): Order[] {

  // TODO: Calculate the resulting orders based on user's portions and maximum price, and available bundles
  // use "getCheapestCombination()" to calculate the resulting bundles by number of orders

  return [];
}